using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using static RedbubbleAPI;

public class SearchManager : MonoBehaviour
{
    /*private Text searchText;
    void Start()
    {
    }
    public void onSubmit()
    {
        searchText = gameObject.transform.GetChild(1).GetComponent<Text>();
        RedbubbleResponse info = RedbubbleAPI.SearchRedbubble(searchText.text);
        foreach (RedbubbleResult result in info.results)
        {   
            GameObject panel = GameObject.find('Panel');
            panel.transform.GetChild(0).GetComponent<Text>().text = result.title;
        }
    }*/
}
